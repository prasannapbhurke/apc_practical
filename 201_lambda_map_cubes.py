# Programs on Lambda Function
# Use map() with lambda to calculate the cube of every element in a list.
# Program: Lambda Map Cubes

numbers = [1, 2, 3, 4, 5]
cubes = list(map(lambda x: x ** 3, numbers))

print(cubes)
