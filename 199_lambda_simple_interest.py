# Programs on Lambda Function
# Create a lambda function to calculate simple interest using principal, rate, and time.
# Program: Lambda Simple Interest

si = lambda p, r, t: (p * r * t) / 100

print(si(1000, 5, 2))
